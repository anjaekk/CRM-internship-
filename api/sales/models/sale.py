from django.db import models

from sales.models.timestamp import TimeStamp

PROGRESS = (
    (0, ''),
    (1, ''),
    (2, ''),
    (3, ''),
    (4, ''),
)

class Sale(TimeStamp):
    product = 
    customer =
    progress = 
